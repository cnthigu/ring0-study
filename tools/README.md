# Ferramentas (opcional)

Scripts de manutencao do repositorio (extracao de texto, figuras, auditorias) podem viver aqui; **nao** sao necessarios para ler as traducoes em `livros/`. Artefactos `__pycache__/` e `*.pyc` ficam ignorados pelo `.gitignore` na raiz.
